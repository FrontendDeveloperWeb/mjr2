// Shared stepper labels for the (new) Overview-first submission flow — see
// paperDraftStore.js for why this flow is kept separate from the older
// step-1..step-6 flow's stepTitles.js. Single source of truth so Step 1 and
// Step 2's <Steps current={N}> index into the same list.
export const OVERVIEW_STEP_TITLES = ['Overview', 'Author', 'Keywords', 'Upload Files', 'Final Submission'];
